// binaryheap.cpp — Binary heap template implementation file.
//
// BinaryHeap is a header-only template class (see
// pgcpp/lib/binaryheap.hpp): all method bodies live in the header so
// callers get implicit instantiation. This translation unit pulls in the
// header so the compiler validates the template definition's syntax as
// part of pgcpp_core.

#include "lib/binaryheap.hpp"

namespace pgcpp::lib {

// No out-of-line definitions: BinaryHeap is header-only.

}  // namespace pgcpp::lib
