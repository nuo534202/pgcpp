// pairingheap.cpp — Pairing heap template implementation file.
//
// PairingHeap is a header-only template class (see
// pgcpp/lib/pairingheap.hpp): all method bodies live in the header so
// callers get implicit instantiation. This translation unit pulls in the
// header so the compiler validates the template definition's syntax as
// part of pgcpp_core.

#include "lib/pairingheap.hpp"

namespace pgcpp::lib {

// No out-of-line definitions: PairingHeap is header-only.

}  // namespace pgcpp::lib
