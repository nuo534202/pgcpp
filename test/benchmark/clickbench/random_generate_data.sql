INSERT INTO hits
SELECT
    (random() * 9223372036854775807)::BIGINT AS WatchID,
    (floor(random() * 2))::SMALLINT AS JavaEnable,
    (CASE WHEN random() < 0.2 THEN'Google Search Results' ELSE'Page Title ' || i END) AS Title,
    (floor(random() * 2))::SMALLINT AS GoodEvent,
    '2013-07-01 00:00:00'::TIMESTAMP + random() * '31 days'::INTERVAL AS EventTime,
    ('2013-07-01'::DATE + (random() * 30)::INT) AS EventDate,
    (CASE WHEN random() < 0.1 THEN 62 ELSE (floor(random() * 1000))::INTEGER END) AS CounterID,
    (floor(random() * 2000000000))::INTEGER AS ClientIP,
    (floor(random() * 100))::INTEGER AS RegionID,
    -- 特意埋入 queries.sql 中点名查询的特定 UserID: 435090932899640449
    (CASE WHEN random() < 0.0001 THEN 435090932899640449 ELSE (random() * 9223372036854775807)::BIGINT END) AS UserID,
    (floor(random() * 5))::SMALLINT AS CounterClass,
    (floor(random() * 10))::SMALLINT AS OS,
    (floor(random() * 100))::SMALLINT AS UserAgent,
    (CASE WHEN random() < 0.15 THEN 'ttps://www.google.com/search?q=test' ELSE'https://example.com/page/' || i END) AS URL,
    (CASE WHEN random() < 0.3 THEN'https://www.google.com/' ELSE'https://referer.com/link/' || i END) AS Referer,
    (floor(random() * 2))::SMALLINT AS IsRefresh,
    (floor(random() * 10))::SMALLINT AS RefererCategoryID,
    (floor(random() * 100))::INTEGER AS RefererRegionID,
    (floor(random() * 10))::SMALLINT AS URLCategoryID,
    (floor(random() * 100))::INTEGER AS URLRegionID,
    (800 + floor(random() * 1120))::SMALLINT AS ResolutionWidth, -- 800 到 1920
    (600 + floor(random() * 480))::SMALLINT AS ResolutionHeight,  -- 600 到 1080
    (floor(random() * 32))::SMALLINT AS ResolutionDepth,
    (floor(random() * 15))::SMALLINT AS FlashMajor,
    (floor(random() * 10))::SMALLINT AS FlashMinor,
    'minor2_' || i AS FlashMinor2,
    (floor(random() * 5))::SMALLINT AS NetMajor,
    (floor(random() * 5))::SMALLINT AS NetMinor,
    (floor(random() * 100))::SMALLINT AS UserAgentMajor,
    'v' || i AS UserAgentMinor,
    (floor(random() * 2))::SMALLINT AS CookieEnable,
    (floor(random() * 2))::SMALLINT AS JavascriptEnable,
    (floor(random() * 2))::SMALLINT AS IsMobile,
    (floor(random() * 10))::SMALLINT AS MobilePhone,
    (CASE WHEN random() < 0.2 THEN'iPhone' WHEN random() < 0.4 THEN'Android' ELSE '' END) AS MobilePhoneModel,
    'param=val_' || i AS Params,
    (floor(random() * 500))::INTEGER AS IPNetworkID,
    (floor(random() * 10))::SMALLINT AS TraficSourceID,
    (floor(random() * 10))::SMALLINT AS SearchEngineID,
    (CASE WHEN random() < 0.4 THEN'search phrase ' || (floor(random() * 50))::INT ELSE '' END) AS SearchPhrase,
    (floor(random() * 5))::SMALLINT AS AdvEngineID, -- 包含 0 和 非 0
    (floor(random() * 2))::SMALLINT AS IsArtifical,
    (800 + floor(random() * 1000))::SMALLINT AS WindowClientWidth,
    (600 + floor(random() * 400))::SMALLINT AS WindowClientHeight,
    (floor(random() * 24))::SMALLINT AS ClientTimeZone,
    '2013-07-01 00:00:00'::TIMESTAMP + random() * '31 days'::INTERVAL AS ClientEventTime,
    (floor(random() * 5))::SMALLINT AS SilverlightVersion1,
    (floor(random() * 5))::SMALLINT AS SilverlightVersion2,
    (floor(random() * 100))::INTEGER AS SilverlightVersion3,
    (floor(random() * 5))::SMALLINT AS SilverlightVersion4,
    'UTF-8' AS PageCharset,
    (floor(random() * 1000))::INTEGER AS CodeVersion,
    (floor(random() * 2))::SMALLINT AS IsLink,
    (floor(random() * 2))::SMALLINT AS IsDownload,
    (floor(random() * 2))::SMALLINT AS IsNotBounce,
    (random() * 9223372036854775807)::BIGINT AS FUniqID,
    'https://example.com/orig/' || i AS OriginalURL,
    (floor(random() * 100000))::INTEGER AS HID,
    (floor(random() * 2))::SMALLINT AS IsOldCounter,
    (floor(random() * 2))::SMALLINT AS IsEvent,
    (floor(random() * 2))::SMALLINT AS IsParameter,
    (CASE WHEN random() < 0.9 THEN 0 ELSE 1 END)::SMALLINT AS DontCountHits, -- 默认为 0 居多
    (floor(random() * 2))::SMALLINT AS WithHash,
    'A'::CHAR AS HitColor,
    '2013-07-01 00:00:00'::TIMESTAMP + random() * '31 days'::INTERVAL AS LocalEventTime,
    (floor(random() * 80))::SMALLINT AS Age,
    (floor(random() * 2))::SMALLINT AS Sex,
    (floor(random() * 4))::SMALLINT AS Income,
    (floor(random() * 20))::SMALLINT AS Interests,
    (floor(random() * 100))::SMALLINT AS Robotness,
    (floor(random() * 2000000000))::INTEGER AS RemoteIP,
    (floor(random() * 10000))::INTEGER AS WindowName,
    (floor(random() * 10000))::INTEGER AS OpenerName,
    (floor(random() * 50))::SMALLINT AS HistoryLength,
    'zh-CN' AS BrowserLanguage,
    'CN' AS BrowserCountry,
    '' AS SocialNetwork,
    '' AS SocialAction,
    (floor(random() * 5))::SMALLINT AS HTTPError,
    (floor(random() * 1000))::INTEGER AS SendTiming,
    (floor(random() * 500))::INTEGER AS DNSTiming,
    (floor(random() * 500))::INTEGER AS ConnectTiming,
    (floor(random() * 1000))::INTEGER AS ResponseStartTiming,
    (floor(random() * 1000))::INTEGER AS ResponseEndTiming,
    (floor(random() * 2000))::INTEGER AS FetchTiming,
    (floor(random() * 10))::SMALLINT AS SocialSourceNetworkID,
    '' AS SocialSourcePage,
    (random() * 100000)::BIGINT AS ParamPrice,
    'order_' || i AS ParamOrderID,
    'USD' AS ParamCurrency,
    (floor(random() * 5))::SMALLINT AS ParamCurrencyID,
    '' AS OpenstatServiceName,
    '' AS OpenstatCampaignID,
    '' AS OpenstatAdID,
    '' AS OpenstatSourceID,
    'google' AS UTMSource,
    'cpc' AS UTMMedium,
    'campaign_' || i AS UTMCampaign,
    '' AS UTMContent,
    '' AS UTMTerm,
    '' AS FromTag,
    (floor(random() * 2))::SMALLINT AS HasGCLID,
    -- 针对特定的 RefererHash 过滤埋点：3594120000172545465
    (CASE WHEN random() < 0.1 THEN 3594120000172545465 ELSE (random() * 9223372036854775807)::BIGINT END) AS RefererHash,
    -- 针对特定的 URLHash 过滤埋点：2868770270353813622
    (CASE WHEN random() < 0.1 THEN 2868770270353813622 ELSE (random() * 9223372036854775807)::BIGINT END) AS URLHash,
    (floor(random() * 10000))::INTEGER AS CLID
FROM generate_series(1, 10000000) AS i;